# Moved

The canonical app catalogue / port registry / feature matrix now lives in
**`~/apps/REGISTRY.md`** (its own repo, bare remote in `~/git/apps.git`).

`~/apps` is the *what-exists* knowledge folder; this webkit folder stays the
*how-to-build* kit. Update REGISTRY.md in the same commit as any port, host,
or lifecycle change.
