# Moved (2026-09-18)

The web-app kit — how to build an app, the keyboard layer snippet, and the
keyboard specification `KEYS.md` — now lives in **`~/apps/kit/`** (repo
`~/apps`, bare remote `~/git/apps.git`), next to `REGISTRY.md`.
